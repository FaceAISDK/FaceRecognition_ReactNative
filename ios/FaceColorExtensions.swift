import Foundation
import SwiftUI

// 1. 如果想改变主题颜色
extension Color {
    /// 主题颜色 #347738 ，用AI换算一下
    static var faceMain: Color {
        return Color(red: 52 / 255.0, green: 119 / 255.0, blue: 56 / 255.0)
    }
}
